import React, { Component } from "react";
import Menu from '../Menu/Menu.js';

export class Home extends Component {
    render(){
        return(
            <div>
                <Menu />
            </div>
        );
    }
}